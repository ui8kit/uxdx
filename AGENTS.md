# UXDX

This repo is the published `uxdx` CLI (operator and SDLC experience packs).

Read `.project/intent.md` before changing product scope. Nested slices live
under `.project/intents/`.

Verify:

```bash
bun test
bun run build
```

Do not invent Codex APIs, product kinds, or a second content REST here.
