# changes/

One directory per change: copy `../intent.md` (optional), `../plan.md` (required)
from the pack root into `./<slug>/` and fill them.

Do not edit the pack-root templates in place once a real change is in flight.
