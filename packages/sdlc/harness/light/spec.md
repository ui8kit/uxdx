# Spec (light — skip by default)

Use this file only when the change touches published information architecture,
a public contract, or a user-visible flow that engineering must plan against.

Otherwise delete it from `.sdlc/changes/<slug>/` and keep `plan.md` only.

## Intent

Link: `../intent.md` or state "no separate intent".

## Requirements

-

## Design notes

-

## Concerns

Flag contradictions with existing `AGENTS.md` / stack-contract. A human resolves
them before implementation.
